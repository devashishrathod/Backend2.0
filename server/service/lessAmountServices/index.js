const { createLessAmount } = require("./createLessAmount");
const { getAllLessAmount } = require("./getAllLessAmounts");
const { generateUniqueLessAmountId } = require("./generateUniqueLessAmountId");

module.exports = {
  createLessAmount,
  getAllLessAmount,
  generateUniqueLessAmountId,
};
