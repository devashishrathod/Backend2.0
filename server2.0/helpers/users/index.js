const { generateReferralCode } = require("./generateReferralCode");
const { generateUniqueUserId } = require("./generateUniqueUserId");

module.exports = { generateReferralCode, generateUniqueUserId };
